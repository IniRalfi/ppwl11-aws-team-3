generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma-pg" // ← Wajib ganti kesini biar nggak makan folder SQLite
}

datasource db {
  provider = "postgresql"
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
